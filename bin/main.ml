(* open ULSynth.Formula;;
   open ULSynth.Program;;
   open ULSynth.ProofRule;; *)

let () = print_endline "main"
(* print_endline (trip_tostr({pre = (Bool true); prog = One; post = (Bool false)}));; *)
(* print_endline (ruleApp_tostr (prove {pre = (Not True); prog = (Numeric (Plus (One, One))); post = (Equals ((TVar ET), (Int 2)))})) *)
